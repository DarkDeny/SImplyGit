<UserControl x:Class="SimplyGit.Views.BrokenRepositoryStatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:SimplyGit.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{Binding Source={d:DesignInstance Type=vm:BrokenRepositoryStatusViewModel, IsDesignTimeCreatable=False}}"
             >
    <TextBlock
        Text="{Binding ErrorMessage}"
        VerticalAlignment="Center"
        Margin="0 5 0 5"
        Foreground="Red"
        FontSize="10"
        />
</UserControl>
